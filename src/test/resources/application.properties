# H2 Database
spring.datasource.url=jdbc:h2:mem:testdb
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

# Hibernate - Auto-generate schema from entities
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=true

# Disable schema.sql/data.sql loading (use entity-based schema instead)
spring.jpa.defer-datasource-initialization=true
spring.sql.init.mode=always
spring.sql.init.data-locations=classpath:data.sql

# JPA/Hibernate
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.H2Dialect

# Disable H2 console in tests
spring.h2.console.enabled=false

# File Upload (FIX for test failure)
file.upload-dir=${java.io.tmpdir}/portfolio-uploads-test
file.static-dir=${java.io.tmpdir}/portfolio-static-test

# Spring Security
jwt.secret=Y6kPN7q3QkGjQqV7H4uPp3q3JwBs7gDp5Sdq8Hr93Fs=
spring.security.enabled=false
